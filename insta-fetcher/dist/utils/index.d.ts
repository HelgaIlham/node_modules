/**
 * format instagram long url to get shortcode
 * @param url a instagram post url
 * @returns shortcode
 */
export declare const shortcodeFormatter: (url: string) => {
    type: string;
    shortcode: string;
};
